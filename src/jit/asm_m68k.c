/* asm_m68k.c — m68k instruction encoders.
 *
 * Skeleton placeholder.  Functions like emit_move_l, emit_jsr,
 * emit_subq_l_imm, emit_beq_forward, bind_label land here as opcode
 * templates need them.
 */

#ifdef JIT_M68K

#include "jit/asm_m68k.h"

/* Translation-unit anchor so the file is never empty under -Wall. */
const char cl_jit_asm_m68k_marker = 0;

#endif /* JIT_M68K */
