; CLAmiga-FPU -- double-click launcher (run by C:IconX, see CLAmiga-FPU.info).
; Starts the hard-float AmigaOS 3 build: REQUIRES a 68881/68882 FPU or a
; 68040/68060 with its FPU library (see README-FIRST, "Which AmigaOS
; binary?").  On MorphOS the native build is the one to run.
Stack 131072
If EXISTS SYS:MorphOS
  bin/mos/clamiga
Else
  bin/aos3-fpu/clamiga
EndIf
